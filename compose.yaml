services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: family_circle
      POSTGRES_USER: family_circle
      POSTGRES_PASSWORD: local_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: